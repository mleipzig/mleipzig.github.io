<!-- vars: category, project -->

<a href="/projects#{{ category.name }}" class="card__">
  <div class="card__title">{{ project.name }}</div>
  <div class="card__desc">{{ project.description }}</div>

  <div class="spacer"></div>

  <div class="card__tag">
    {{ category.name }}
  </div>
</a>

